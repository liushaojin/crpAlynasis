CREATE TABLE `mv200`.`projectt` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(15) NOT NULL,
  `batchNum` VARCHAR(20),
  `isDiluent`,
  `unit` VARCHAR(10) NOT NULL DEFAULT 'mg/L',
  `accuracy` INT NOT NULL DEFAULT 2,
  `lowLinear` DOUBLE ZEROFILL NULL,
  `K` DOUBLE NOT NULL DEFAULT 1,
  `bloodFactor` DOUBLE NOT NULL DEFAULT 1,
  `lightPos1` INT NOT NULL DEFAULT 3,
  `lightPos2` INT NOT NULL DEFAULT 10,
  `r0ID` INT ZEROFILL NULL DEFAULT 0,
  `r1ID` INT ZEROFILL NULL DEFAULT 0,
  `r2ID` INT ZEROFILL NULL DEFAULT 0,
  `sampleVol` INT ZEROFILL NULL,
  `r0Vol` INT ZEROFILL NULL,
  `r1Vol` INT ZEROFILL NULL,
  `rsVol` INT ZEROFILL NULL,
  `r2Vol` INT ZEROFILL NULL,
  `testMethodID` INT NULL DEFAULT 1,
  `reactionDirID` INT NULL DEFAULT 1,
  `calibrateID` INT NULL DEFAULT 1,
  `calibrate1_R0` DOUBLE NULL,
  `calibrate1_K` DOUBLE NULL,
  `calibrate1_a` DOUBLE NULL,
  `calibrate1_b` DOUBLE NULL,
  `calibrate1_c` DOUBLE NULL,
  `calibrate1_RR` DOUBLE NULL,
  `calibrate1_CC` DOUBLE NULL,
  `isCalibrate2` INT NOT NULL DEFAULT 0,
  `calibrate2_R0` DOUBLE ZEROFILL NULL,
  `calibrate2_K` DOUBLE ZEROFILL NULL,
  `calibrate2_a` DOUBLE ZEROFILL NULL,
  `calibrate2_b` DOUBLE ZEROFILL NULL,
  `calibrate2_c` DOUBLE ZEROFILL NULL,
  `calibrate2_RR` DOUBLE ZEROFILL NULL,
  `calibrate2_CC` DOUBLE ZEROFILL NULL,
  `ProjectTcol` VARCHAR(45) NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `id_UNIQUE` (`id` ASC),
  UNIQUE INDEX `name_UNIQUE` (`name` ASC));
